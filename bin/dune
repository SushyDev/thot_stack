(executable
 (name main)
 (public_name main)
 (modules main)
 (libraries layouts pages home about project todo dream tyxml lwt lib))

; (executable (name runapp)
;   (libraries supervisor)
;   (modules Runapp)
;   (flags (:standard -g))
;   (preprocess (pps ppx_deriving.std)))

(rule
  (alias watch)
  (deps main.exe)
  (action (run ./main.exe args)))
